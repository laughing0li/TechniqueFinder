# app engine config file

runtime: php74

env_variables:
  CLOUDSQL_CONNECTION_NAME: tf-4-agn:australia-southeast1:tfinder
  CLOUDSQL_USER: root
  CLOUDSQL_PASSWORD: tfinder
  CLOUDSQL_DATABASE_NAME: techfinder

handlers:
  - url: /assets
    static_dir: assets
  - url: /media-dir
    static_dir: media-dir